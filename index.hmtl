<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Something From My Heart ❤️</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #3b0000, #7a0c0c);
      display: flex;
      justify-content: center;
      align-items: center;
      color: #fff;
      overflow: hidden;
    }

    .card {
      background: #5a0a0a;
      border-radius: 22px;
      padding: 40px;
      text-align: center;
      box-shadow: 0 25px 45px rgba(0,0,0,0.5);
      max-width: 420px;
      position: relative;
      animation: fadeIn 1.3s ease;
    }

    h1 {
      font-size: 2.1rem;
      color: #ffd6d6;
      margin-bottom: 15px;
    }

    .message {
      font-size: 1.05rem;
      color: #ffecec;
      line-height: 1.6;
      margin-bottom: 25px;
    }

    .question {
      font-size: 1.5rem;
      margin-bottom: 25px;
      color: #ffb3b3;
      font-weight: bold;
    }

    .buttons {
      display: flex;
      justify-content: center;
      gap: 20px;
    }

    button {
      border: none;
      padding: 12px 26px;
      font-size: 1rem;
      border-radius: 25px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    #yesBtn {
      background: #b11226;
      color: white;
    }

    #yesBtn:hover {
      background: #d4142f;
      transform: scale(1.15);
    }

    #noBtn {
      background: #2b0000;
      color: #ffd6d6;
      position: relative;
    }

    #finalMessage {
      margin-top: 25px;
      font-size: 1.3rem;
      color: #ffd6d6;
      display: none;
    }

    /* Characters */
    .character {
      position: absolute;
      font-size: 2.2rem;
      animation: bounce 2.5s infinite;
    }

    .char-left {
      left: -20px;
      bottom: -20px;
    }

    .char-right {
      right: -20px;
      top: -20px;
      animation-delay: 1.2s;
    }

    /* Floating hearts */
    .heart {
      position: absolute;
      color: #ff4d6d;
      font-size: 20px;
      animation: float 4s linear infinite;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes float {
      from {
        transform: translateY(100vh) scale(1);
        opacity: 1;
      }
      to {
        transform: translateY(-10vh) scale(1.5);
        opacity: 0;
      }
    }

    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }
  </style>
</head>
<body>

  <div class="card">
    <!-- Cute characters -->
    <div class="character char-left">🧸❤️</div>
    <div class="character char-right">🐻‍❣️</div>

    <h1>Hey you ❤️</h1>

    <div class="message">
      I don’t know how to put this perfectly into words,<br>
      but being with you makes everything feel lighter.<br><br>

      I catch myself smiling over the smallest things because of you,
      and honestly… you mean more to me than I ever expected.<br><br>

      I feel safe with you. I feel happy with you.
      And I’d really love to see where this could go.
    </div>

    <div class="question">
      Can I be your girlfriend? 💌
    </div>

    <div class="buttons">
      <button id="yesBtn">Yes 💖</button>
      <button id="noBtn">No 🙈</button>
    </div>

    <div id="finalMessage">
      You just made my heart so full 🥰  
      I’m really excited for us ❤️
    </div>
  </div>

  <!-- Background Music -->
  <iframe
    id="music"
    width="0"
    height="0"
    src="https://www.youtube.com/embed/ZhIsAZO5gl0?autoplay=0&loop=1&playlist=ZhIsAZO5gl0"
    frameborder="0"
    allow="autoplay">
  </iframe>

  <script>
    const yesBtn = document.getElementById("yesBtn");
    const noBtn = document.getElementById("noBtn");
    const finalMessage = document.getElementById("finalMessage");
    const music = document.getElementById("music");

    let musicStarted = false;

    function startMusic() {
      if (!musicStarted) {
        music.src += "&autoplay=1";
        musicStarted = true;
      }
    }

    yesBtn.addEventListener("click", () => {
      startMusic();
      finalMessage.style.display = "block";
      createHearts();
    });

    noBtn.addEventListener("mouseover", () => {
      startMusic();
      const x = Math.random() * 220 - 110;
      const y = Math.random() * 220 - 110;
      noBtn.style.transform = `translate(${x}px, ${y}px)`;
    });

    function createHearts() {
      for (let i = 0; i < 30; i++) {
        const heart = document.createElement("div");
        heart.classList.add("heart");
        heart.innerHTML = "❤️";
        heart.style.left = Math.random() * 100 + "vw";
        heart.style.animationDuration = (Math.random() * 2 + 3) + "s";
        document.body.appendChild(heart);

        setTimeout(() => heart.remove(), 4000);
      }
    }
  </script>

</body>
</html>
